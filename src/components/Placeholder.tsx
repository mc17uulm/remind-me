import React from "react";
import { Placeholder as PlaceholderContainer } from "semantic-ui-react";

interface PlaceholderProps {
    height: number
}

export const Placeholder = () => {

    return (
        <PlaceholderContainer>

        </PlaceholderContainer>
    )

}